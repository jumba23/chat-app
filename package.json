{
    "name": "chat-app",
    "version": "1.0.0",
    "scripts": {
        "start": "npm install --prefix chat-app-backend && npm start --prefix chat-app-backend && npm install --prefix chat-app-frontend && npm run build --prefix chat-app-frontend"
       }
  }